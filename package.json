{
  "name": "kospi-risk-watch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Public personal non-advisory KOSPI Monday downside and KOSPI200 expiry-settlement dashboard MVP.",
  "scripts": {
    "dev": "node apps/api/src/server.js",
    "start": "node apps/api/src/server.js",
    "lint": "node scripts/lint.mjs",
    "typecheck": "node scripts/typecheck.mjs",
    "test": "node --test",
    "verify": "npm run lint && npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
